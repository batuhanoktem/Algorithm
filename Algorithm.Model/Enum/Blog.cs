namespace Algorithm.Model.Enum
{
    public enum Blog
    {
    }
    public enum Status
    {
        Draft,
        Publish
    }
    public enum Visibility
    {
        Public,
        Private
    }
}

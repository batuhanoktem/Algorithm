namespace Algorithm.Model.Enum
{
    public enum Blog
    {
    }
    public enum Status
    {
        Draft = 1,
        Publish = 2,
    }
    public enum Visibility
    {
        Public = 1,
        Private = 2,
    }
}
